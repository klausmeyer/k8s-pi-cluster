apiVersion: v2
name: kubernetes-dashboard
description: Deployment chart for kubernetes-dashboard
type: application
version: "0.0.0"

dependencies:
  - name: kubernetes-dashboard
    version: "7.10.5"
    repository: https://kubernetes.github.io/dashboard
