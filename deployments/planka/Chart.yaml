apiVersion: v2
name: planka
description: Deployment chart for planka
type: application
version: "0.0.0"

dependencies:
  - name: planka
    version: "2.0.1"
    repository: http://plankanban.github.io/planka
